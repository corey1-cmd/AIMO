# CHANGELOG — patch-v5 vs FEEDBACK_REPORT

본 문서는 시니어 2가 작성한 FEEDBACK_REPORT (v1.0)에서 출발하여 시니어 3이 실제 구현 단계에서 적용한 수정 사항과, 튜닝 과정에서 추가로 발생한 변경을 정리합니다.

## Day 0.5 Baseline에 근거한 원안 수정 (시니어 2 승인)

FEEDBACK_REPORT는 "L4_direct = distortion 주 원인"이라는 전제 위에서 `LEVEL_WEIGHT.L4_direct = 0.35`를 핵심 조치로 삼았습니다. Baseline 측정 결과 L4_direct 매칭이 213 케이스 전체에서 한 건도 발생하지 않았고, distortion의 67%가 L3_partial에서 발생하는 것이 확인되었습니다. 이에 따라 L4_direct penalty는 0.6으로 완화하여 미래 방어용으로만 유지하였고, 실질 방어의 1차 가드는 L3_partial에 배치했습니다. 시니어 3이 제시한 7개 수정안 중 5개가 전면 채택되고 2개가 부분 채택되었습니다.

전면 채택된 것은 Day 0.5 baseline 측정 추가, L3_partial의 2단계 분기 처리, `_medical_work_` sentinel 대신 `reject: true` 플래그 사용, legacy_bridge의 `confidence_cap` 대신 `confidence_floor` 사용, 그리고 waterfall 일정을 incremental 일정으로 재배치한 것입니다. 부분 채택된 것은 ABSTRACT_VERBS를 8개 고정 리스트로 축소하되 동적 POS 태깅은 YAGNI 원칙에 따라 제외한 것입니다. "작성"은 "논문 작성" 같은 내적 메모를 배제하지 않기 위해 리스트에서 빠졌습니다.

## legacy_bridge의 V3 L2 코드 교정 (시니어 2 누락 사항)

FEEDBACK_REPORT가 제안한 legacy_bridge 초안은 `financial_admin`, `work_admin`, `research_work`, `study_engineering`, `study_k12` 등 V3에 존재하지 않는 L2 코드를 사용하고 있었습니다. 시니어 3이 V3 `L2_META`를 대조하여 5건의 target_l2를 실제 존재하는 코드로 교정하였습니다. 각각 `banking_routine`, `work_planning`, `data_analysis_res`, `study_solo`, `exam_prep`로 매핑되었으며, 시니어 2가 전건 승인하였습니다. V3에 coding 전용 L2가 존재하지 않는 문제는 v4 로드맵으로 기록되었고, 현재는 IT 관련 키워드 대부분을 `work_core`로 라우팅합니다.

## 튜닝 단계에서 추가된 변경

회귀 테스트를 돌리면서 다음 세 가지 추가 변경이 발생했습니다.

단일 토큰 입력에 대한 0.75 confidence cap이 원안에서는 모든 매칭 레벨에 일괄 적용되도록 설계되어 있었으나, "샤워"나 "양치" 같이 `L3_exact`로 정확 매칭되는 단일 토큰이 0.85 min_confidence 요구를 통과하지 못하는 문제가 드러났습니다. Cap을 `L3_partial`과 `L2_fallback`에만 적용하도록 범위를 좁혔고, 이로 인해 A 카테고리 통과율이 17%에서 58%로 상승했습니다.

Coherence validator의 L1_HINTS 정규식에 있던 `\b` 워드 바운더리가 한글에서 작동하지 않아 디지털 관련 힌트가 전혀 발화하지 않는 문제가 있었습니다. 워드 바운더리를 제거하고 "로드맵", "CRM", "경쟁사" 등 업무 일반 힌트 목록을 추가하여 distortion이 5건에서 2건으로 감소했습니다.

마지막 2건의 distortion은 parser-v3의 `pickL4FromL3`가 `deploy_release` L3에 대해 `hotfix`를 기본 L4로 선택하는 것이 원인이었습니다. L3 분류 자체는 정확했지만 L4 태그가 "긴급 수정"의 의미를 갖는 것이 문제였습니다. parser-v3 내부를 수정하는 대신 coherence validator에 3차 가드를 추가하여 오해 유도적 L4 기본값(`hotfix`, `activity_date`, `pet_grooming_self`)이 urgency/context 마커 없이 선택되는 것을 차단했습니다. 이로써 distortion이 0건에 도달했습니다.

## 최종 회귀 결과 및 체크포인트 응답

최종 회귀 결과는 213 케이스 중 90건 통과(42.3%), distortion 0건입니다. 시니어 2가 요청한 세 가지 체크포인트에 대한 응답은 다음과 같습니다.

첫째, coherence validator와 legacy_bridge 사이의 L1 충돌은 7건이 관측되었습니다. 모두 IT 개발 관련 키워드(API, DB, CI, PR, 테스트 코드, 로그, 스프린트)에서 validator는 `digital_creation`을 기대하지만 legacy_bridge는 `work_core`로 라우팅하여 L1이 `work`로 보고됩니다. v1.1 fixture가 이들 케이스를 `any_of: ["digital_creation", "work"]`로 완화해둔 덕에 전건 통과합니다. 이 충돌은 V3에 coding 전용 L2가 없기 때문이며, 파이프라인 결함이 아닌 V3 taxonomy 갭의 표출입니다.

둘째, distortion은 baseline 21에서 0으로 감소했습니다. 목표 달성입니다.

셋째, A 카테고리 통과율은 58.3%로 목표 90%에 미달합니다. 실패 25건은 모두 V3 파서의 coverage 누락이며, "머리 감기", "샴푸", "드라이", "자기", "쪽잠", "늦잠", "요리", "물 마시기" 등 파서가 전혀 인식하지 못하는 일상 표현이 대부분입니다. 3-layer 파이프라인은 파서가 생성하지 않은 매칭을 만들어낼 수 없으므로, 이는 시니어 2가 Day 6로 명시적으로 지정한 SYNONYMS 확장 작업에서 해결되어야 합니다.

## v4 로드맵으로 이관된 항목

V3 L3_META의 명명 충돌 문제(`pet_grooming_self.name = "관리"`, `food_storage.name = "정리"`)는 baseline에서 distortion 15건의 근본 원인으로 확인되었으나, 현재 3-layer 가드로 증상을 억제하는 수준입니다. 근본 치료는 V3 taxonomy에서 해당 L3 이름을 "반려동물 털 손질", "식품 저장 정리"로 명시화하는 것이며, v4 로드맵 최상단 항목입니다.

V3에 coding 전용 L2가 없는 문제도 v4 로드맵으로 이관되었습니다. 현재 legacy_bridge는 모든 IT 개발 키워드를 `work_core`로 라우팅하는데, 의미적으로 거칠기는 하지만 틀리지는 않습니다. v4에서 `coding_dev` L2를 추가하면 자연스러운 해결이 가능합니다.

parser-v3의 `pickL4FromL3`가 misleading default를 반환하는 문제는 현재 validator 3차 가드로 억제되지만, 본질적으로는 파서의 L4 선택 로직 자체가 각 L3별 기본값 테이블을 가져야 합니다. 이 역시 v4 로드맵 후속 작업입니다.

## Distortion 감소 추이

| 단계 | Distortion | Pass rate |
|---|---:|---:|
| Baseline (파서만) | 21 | 38.5% |
| Day 3 wire-up (3-layer 최초 통합) | 5 | 29.1% |
| 단일토큰 cap 범위 교정 | 5 | 40.8% |
| L1_HINTS 워드바운더리 제거 + 업무 힌트 추가 | 2 | 41.3% |
| L4 태그 오해 유도 가드 추가 | 0 | 42.3% |

통과율이 baseline과 유사한 수준인 것은 3-layer 파이프라인이 잘못된 매칭을 걸러내는 데 주력하기 때문입니다. Pass rate 상승은 Day 6 SYNONYMS 확장에서 본격적으로 이루어질 것입니다. Distortion 0을 달성한 것이 patch-v5의 핵심 성과입니다.
